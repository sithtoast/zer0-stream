name: Publish API Image

on:
  push:
    branches: [main]
    paths:
      - "zer0_stream/**"
      - ".github/workflows/publish-api.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: publish-api-${{ github.ref }}
  cancel-in-progress: true

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKER_USERNAME }}/zer0-stream
          tags: |
            type=sha,prefix={{branch}}-
            type=ref,event=branch
            type=raw,value=latest

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: zer0_stream
          file: zer0_stream/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=zer0-stream
          cache-to: type=gha,mode=max,scope=zer0-stream

  deploy:
    needs: publish
    runs-on: ubuntu-latest

    steps:
      - name: Notify Portainer
        env:
          PORTAINER_WEBHOOK_URL: ${{ secrets.PORTAINER_WEBHOOK_URL }}
        run: |
          test -n "$PORTAINER_WEBHOOK_URL"
          curl --fail-with-body -k -X POST "$PORTAINER_WEBHOOK_URL" \
            -H "Content-Type: application/json" \
            -d '{
              "status": "success",
              "images": ["${{ secrets.DOCKER_USERNAME }}/zer0-stream:latest"],
              "commit": "${{ github.sha }}",
              "branch": "${{ github.ref_name }}"
            }'
